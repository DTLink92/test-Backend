package com.dh.spring5webapp.model;

import org.junit.Test;

import static org.junit.Assert.*;

public class RecruitmentProfileTest {

    private static final String requirement = "requirement";
    private static final String description = "description";
    private static final String verification = "verificacion";

    @Test
    public void getRequirement() {
        /*RecruitmentProfile profile = new RecruitmentProfile();
        profile.setRequirement(requirement);
        assertEquals(requirement, profile.getRequirement());*/
        assertEquals(true, true);
    }

    /*
    @Test
    public void getDescription() {
        RecruitmentProfile profile = new RecruitmentProfile();
        profile.setDescription(description);
        assertEquals(description, profile.getDescription());
    }

    @Test
    public void getVerification() {
        RecruitmentProfile profile = new RecruitmentProfile();
        profile.setVerification(verification);
        assertEquals(verification, profile.getVerification());
    }

    @Test
    public void getPosition() {

    }*/
}