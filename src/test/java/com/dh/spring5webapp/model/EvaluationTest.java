package com.dh.spring5webapp.model;

import org.junit.Test;

import java.util.LinkedList;
import java.util.List;

import static org.junit.Assert.*;

public class EvaluationTest {

    @Test
    public void getEvaluated() {
        /*Evaluation evaluation = new Evaluation();
        Employee employee = new Employee();
        Long id = new Long(1);
        employee.setId(id);
        evaluation.setEvaluated(employee);
        assertEquals(id, employee.getId());*/
    }
    /*
    @Test
    public void getEvaluator() {
        Evaluation evaluation = new Evaluation();
        Employee employee = new Employee();
        Long id = new Long(1);
        employee.setId(id);
        evaluation.setEvaluator(employee);
        assertEquals(id, employee.getId());
    }

    @Test
    public void getType() {
        Evaluation evaluation = new Evaluation();
        TypeEvaluator type = new TypeEvaluator();
        Long id = new Long(1);
        type.setId(id);
        evaluation.setType(type);
        assertEquals(id, type.getId());
    }

    @Test
    public void getIndicatorValues() {
        Evaluation evaluation = new Evaluation();
        List<IndicatorValue> indicators = new LinkedList<>();
        IndicatorValue indicatorValue = new IndicatorValue();
        Long id =  new Long(1);
        indicatorValue.setId(id);
        indicators.add(indicatorValue);
        evaluation.setIndicatorValues(indicators);
        assertEquals(id, evaluation.getIndicatorValues().get(1).getId());

    }*/
}